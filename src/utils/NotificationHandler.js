import messaging from '@react-native-firebase/messaging';

messaging().setBackgroundMessageHandler(async (notification) => {
  
});

export const handleNotificationPress = (remoteMessage, feedBack = false) => {
  
};

export const handleMessage = (notification) => {
 
};

export const handleInitialNotification = (remoteMessage) => {
 
};

export const handleNotificationOpenedApp = (remoteMessage) => {
  
};
